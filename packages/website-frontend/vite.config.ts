// vite.config.ts
export default {
  build: {
    rollupOptions: {
      // input: {
      //   // entry point for compilation; normally would be "./index.html"
      //   app: './src/index.html',
      // }
    }
  }
}
